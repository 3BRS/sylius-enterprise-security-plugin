{% if hookable_metadata.context.form.user.plainPassword is defined %}
    {{ form_row(hookable_metadata.context.form.user.plainPassword, sylius_test_form_attribute('password')) }}
{% else %}
    <div class="alert alert-info" role="alert">
        {{ 'three_brs.ui.password_login.customer_account_without_password'|trans }}
    </div>
{% endif %}
