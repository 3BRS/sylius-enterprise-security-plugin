{% extends '@SyliusAdmin/shared/layout/base.html.twig' %}

{% from '@SyliusAdmin/shared/helper/breadcrumbs.html.twig' import breadcrumbs %}
{% import '@SyliusAdmin/shared/helper/header.html.twig' as header %}

{% form_theme form '@SyliusAdmin/shared/form_theme.html.twig' %}

{% block title %}{{ parent() }} | {{ 'three_brs.ui.security_settings.title'|trans }}{% endblock %}

{% block body %}
    {% include '@SyliusAdmin/shared/crud/common/sidebar.html.twig' %}

    <div class="page-wrapper">
        {% include '@SyliusAdmin/shared/crud/common/navbar.html.twig' %}

        <div class="page-header" data-sticky-header>
            <div class="container-xl">
                {{ breadcrumbs([
                    { name: 'sylius.ui.dashboard', url: path('sylius_admin_dashboard'), active: false },
                    { name: 'three_brs.ui.security_settings.title', active: true },
                ]) }}
                <div class="row mt-3 row-gap-2 align-items-center">
                    <div class="col">
                        {{ header.h1('three_brs.ui.security_settings.title'|trans) }}
                        <p class="text-secondary mb-0">{{ 'three_brs.ui.security_settings.intro'|trans }}</p>
                    </div>
                    <div class="col-auto">
                        <button type="submit"
                                form="three-brs-security-settings-form"
                                class="btn btn-primary"
                                data-test-three-brs-security-settings-save>
                            {{ 'three_brs.ui.security_settings.save'|trans }}
                        </button>
                    </div>
                </div>
            </div>
        </div>

        {% include '@SyliusAdmin/shared/crud/common/content/flashes.html.twig' %}

        <div class="page-body">
            <div class="container-xl">
                <div class="card mb-3">
                    <div class="card-body">
                        <ul class="nav nav-pills" data-test-three-brs-security-settings-scope>
                            <li class="nav-item">
                                <a class="nav-link {{ scope_value == 'customer' ? 'active' : '' }}"
                                   href="{{ path('three_brs_admin_security_settings_index', {'scope': 'customer'}) }}"
                                   data-test-three-brs-security-settings-scope-customer>
                                    {{ 'three_brs.ui.security_settings.scope_customer'|trans }}
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link {{ scope_value == 'admin' ? 'active' : '' }}"
                                   href="{{ path('three_brs_admin_security_settings_index', {'scope': 'admin'}) }}"
                                   data-test-three-brs-security-settings-scope-admin>
                                    {{ 'three_brs.ui.security_settings.scope_admin'|trans }}
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>

                {{ form_start(form, {'attr': {'id': 'three-brs-security-settings-form', 'novalidate': 'novalidate'}}) }}
                    {{ form_errors(form) }}
                    <div class="three-brs-security-settings-masonry">
                        {% for tab in form.children %}
                            {% if tab.vars.name != '_token' %}
                                <div class="card mb-3 three-brs-security-settings-masonry-item"
                                     data-test-three-brs-security-settings-tab="{{ tab.vars.name }}">
                                    <div class="card-header">
                                        <h3 class="card-title mb-0">{{ ('three_brs.ui.security_settings.' ~ tab.vars.name ~ '.title')|trans }}</h3>
                                    </div>
                                    <div class="card-body">
                                        <p class="text-secondary mb-4">{{ ('three_brs.ui.security_settings.' ~ tab.vars.name ~ '.description')|trans }}</p>
                                        {% if tab.vars.disabled %}
                                            <div class="alert alert-info mb-4"
                                                 data-test-three-brs-password-settings-disabled-note>
                                                {{ 'three_brs.ui.security_settings.password_settings_disabled_note'|trans }}
                                            </div>
                                        {% endif %}
                                        {{ form_widget(tab) }}
                                        {{ form_errors(tab) }}
                                    </div>
                                </div>
                            {% endif %}
                        {% endfor %}
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

    <link rel="stylesheet" href="{{ asset('bundles/threebrssyliusenterprisesecurityplugin/css/security_settings_masonry.css') }}">
{% endblock %}
