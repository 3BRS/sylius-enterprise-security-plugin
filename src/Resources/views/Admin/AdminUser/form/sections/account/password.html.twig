{% if hookable_metadata.context.form.plainPassword is defined %}
    {{ form_row(hookable_metadata.context.form.plainPassword) }}
{% else %}
    <div class="alert alert-info" role="alert">
        {{ 'three_brs.ui.password_login.admin_account_without_password'|trans }}
    </div>
{% endif %}
