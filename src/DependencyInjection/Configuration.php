<?php

declare(strict_types=1);

namespace ThreeBRS\SyliusEnterpriseSecurityPlugin\DependencyInjection;

use Symfony\Component\Config\Definition\Builder\TreeBuilder;
use Symfony\Component\Config\Definition\ConfigurationInterface;

class Configuration implements ConfigurationInterface
{
    /**
     * @return TreeBuilder<'array'>
     */
    public function getConfigTreeBuilder(): TreeBuilder
    {
        $treeBuilder = new TreeBuilder('three_brs_sylius_enterprise_security');
        $rootNode = $treeBuilder->getRootNode();

        $rootNode
            ->children()
                ->arrayNode('password_history')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->arrayNode('customer')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->booleanNode('enabled')->defaultFalse()->end()
                                ->integerNode('count')->defaultValue(5)->min(1)->end()
                            ->end()
                        ->end()
                        ->arrayNode('admin')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->booleanNode('enabled')->defaultFalse()->end()
                                ->integerNode('count')->defaultValue(10)->min(1)->end()
                            ->end()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('password_expiration')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->arrayNode('customer')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->booleanNode('enabled')->defaultFalse()->end()
                                ->integerNode('days')->defaultValue(90)->min(1)->end()
                            ->end()
                        ->end()
                        ->arrayNode('admin')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->booleanNode('enabled')->defaultFalse()->end()
                                ->integerNode('days')->defaultValue(60)->min(1)->end()
                            ->end()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('password_change_notification')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->arrayNode('customer')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->booleanNode('enabled')->defaultFalse()->end()
                            ->end()
                        ->end()
                        ->arrayNode('admin')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->booleanNode('enabled')->defaultFalse()->end()
                            ->end()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('two_factor_authentication')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->scalarNode('issuer')->defaultValue('Sylius')->cannotBeEmpty()->end()
                        ->arrayNode('customer')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->enumNode('mode')
                                    ->values(['disabled', 'optional', 'enforced'])
                                    ->defaultValue('disabled')
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('admin')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->enumNode('mode')
                                    ->values(['disabled', 'optional', 'enforced'])
                                    ->defaultValue('disabled')
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('recovery_codes')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->arrayNode('customer')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->booleanNode('enabled')->defaultTrue()->end()
                                        ->integerNode('count')->defaultValue(8)->min(1)->end()
                                    ->end()
                                ->end()
                                ->arrayNode('admin')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->booleanNode('enabled')->defaultTrue()->end()
                                        ->integerNode('count')->defaultValue(8)->min(1)->end()
                                    ->end()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('trusted_device')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->booleanNode('enabled')->defaultTrue()->end()
                                ->integerNode('days')->defaultValue(60)->min(1)->end()
                            ->end()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('password_policy')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->arrayNode('customer')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->integerNode('min_length')->defaultValue(8)->min(1)->end()
                                ->scalarNode('max_length')->defaultNull()->validate()->ifTrue(static fn ($v) => $v !== null && (!is_int($v) || $v < 1))->thenInvalid('max_length must be a positive integer or null')->end()->end()
                                ->booleanNode('require_uppercase')->defaultFalse()->end()
                                ->booleanNode('require_lowercase')->defaultFalse()->end()
                                ->booleanNode('require_numbers')->defaultFalse()->end()
                                ->booleanNode('require_special_characters')->defaultFalse()->end()
                            ->end()
                        ->end()
                        ->arrayNode('admin')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->integerNode('min_length')->defaultValue(12)->min(1)->end()
                                ->scalarNode('max_length')->defaultNull()->validate()->ifTrue(static fn ($v) => $v !== null && (!is_int($v) || $v < 1))->thenInvalid('max_length must be a positive integer or null')->end()->end()
                                ->booleanNode('require_uppercase')->defaultTrue()->end()
                                ->booleanNode('require_lowercase')->defaultTrue()->end()
                                ->booleanNode('require_numbers')->defaultTrue()->end()
                                ->booleanNode('require_special_characters')->defaultTrue()->end()
                            ->end()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('magic_link')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->arrayNode('customer')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->booleanNode('enabled')->defaultFalse()->end()
                                ->integerNode('expiration_seconds')->defaultValue(300)->min(60)->end()
                            ->end()
                        ->end()
                        ->arrayNode('admin')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->booleanNode('enabled')->defaultFalse()->end()
                                ->integerNode('expiration_seconds')->defaultValue(300)->min(60)->end()
                            ->end()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('passkey')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->scalarNode('rp_id')->defaultNull()->end()
                        ->scalarNode('rp_name')->defaultNull()->end()
                        ->booleanNode('skip_2fa_when_user_verified')->defaultFalse()->end()
                        ->arrayNode('customer')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->booleanNode('enabled')->defaultFalse()->end()
                            ->end()
                        ->end()
                        ->arrayNode('admin')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->booleanNode('enabled')->defaultFalse()->end()
                            ->end()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('account_lockout')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->arrayNode('customer')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->booleanNode('enabled')->defaultFalse()->end()
                                ->integerNode('max_attempts')->defaultValue(5)->min(1)->end()
                                ->integerNode('auto_unlock_after')->defaultNull()->min(1)->end()
                            ->end()
                        ->end()
                        ->arrayNode('admin')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->booleanNode('enabled')->defaultFalse()->end()
                                ->integerNode('max_attempts')->defaultValue(3)->min(1)->end()
                                ->integerNode('auto_unlock_after')->defaultNull()->min(1)->end()
                            ->end()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('rate_limit')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->arrayNode('customer')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->arrayNode('login')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->booleanNode('enabled')->defaultFalse()->end()
                                        ->integerNode('limit')->defaultValue(5)->min(1)->end()
                                        ->scalarNode('interval')->defaultValue('15 minutes')->cannotBeEmpty()->end()
                                    ->end()
                                ->end()
                                ->arrayNode('password_reset')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->booleanNode('enabled')->defaultFalse()->end()
                                        ->integerNode('limit')->defaultValue(3)->min(1)->end()
                                        ->scalarNode('interval')->defaultValue('1 hour')->cannotBeEmpty()->end()
                                    ->end()
                                ->end()
                                ->arrayNode('register')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->booleanNode('enabled')->defaultFalse()->end()
                                        ->integerNode('limit')->defaultValue(5)->min(1)->end()
                                        ->scalarNode('interval')->defaultValue('1 hour')->cannotBeEmpty()->end()
                                    ->end()
                                ->end()
                                ->arrayNode('magic_link')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->booleanNode('enabled')->defaultFalse()->end()
                                        ->integerNode('limit')->defaultValue(3)->min(1)->end()
                                        ->scalarNode('interval')->defaultValue('15 minutes')->cannotBeEmpty()->end()
                                    ->end()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('admin')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->arrayNode('login')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->booleanNode('enabled')->defaultFalse()->end()
                                        ->integerNode('limit')->defaultValue(5)->min(1)->end()
                                        ->scalarNode('interval')->defaultValue('15 minutes')->cannotBeEmpty()->end()
                                    ->end()
                                ->end()
                                ->arrayNode('password_reset')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->booleanNode('enabled')->defaultFalse()->end()
                                        ->integerNode('limit')->defaultValue(3)->min(1)->end()
                                        ->scalarNode('interval')->defaultValue('1 hour')->cannotBeEmpty()->end()
                                    ->end()
                                ->end()
                                ->arrayNode('magic_link')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->booleanNode('enabled')->defaultFalse()->end()
                                        ->integerNode('limit')->defaultValue(3)->min(1)->end()
                                        ->scalarNode('interval')->defaultValue('15 minutes')->cannotBeEmpty()->end()
                                    ->end()
                                ->end()
                            ->end()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('oauth')
                    ->addDefaultsIfNotSet()
                    ->children()
                        ->arrayNode('customer')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->arrayNode('google')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->booleanNode('enabled')->defaultFalse()->end()
                                        ->scalarNode('client_id')->defaultNull()->end()
                                        ->scalarNode('client_secret')->defaultNull()->end()
                                    ->end()
                                ->end()
                                ->arrayNode('apple')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->booleanNode('enabled')->defaultFalse()->end()
                                        ->scalarNode('client_id')->defaultNull()->end()
                                        ->scalarNode('team_id')->defaultNull()->end()
                                        ->scalarNode('key_id')->defaultNull()->end()
                                        ->scalarNode('private_key_path')->defaultNull()->end()
                                    ->end()
                                ->end()
                            ->end()
                        ->end()
                        ->arrayNode('admin')
                            ->addDefaultsIfNotSet()
                            ->children()
                                ->scalarNode('default_locale')->defaultValue('en_US')->cannotBeEmpty()->end()
                                ->arrayNode('auto_register_allowed_email_domains')
                                    ->scalarPrototype()->end()
                                    ->defaultValue([])
                                ->end()
                                ->arrayNode('google')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->booleanNode('enabled')->defaultFalse()->end()
                                        ->scalarNode('client_id')->defaultNull()->end()
                                        ->scalarNode('client_secret')->defaultNull()->end()
                                    ->end()
                                ->end()
                                ->arrayNode('apple')
                                    ->addDefaultsIfNotSet()
                                    ->children()
                                        ->booleanNode('enabled')->defaultFalse()->end()
                                        ->scalarNode('client_id')->defaultNull()->end()
                                        ->scalarNode('team_id')->defaultNull()->end()
                                        ->scalarNode('key_id')->defaultNull()->end()
                                        ->scalarNode('private_key_path')->defaultNull()->end()
                                    ->end()
                                ->end()
                            ->end()
                        ->end()
                    ->end()
                ->end()
            ->end()
        ;

        return $treeBuilder;
    }
}
